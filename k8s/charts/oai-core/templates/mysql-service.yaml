apiVersion: v1
kind: Service
metadata:
  name: oai-mysql
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: oai-mysql
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
      protocol: TCP
