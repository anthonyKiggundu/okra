apiVersion: v1
kind: Service
metadata:
  name: oai-udm
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "oai-core-cn5g.componentLabels" (dict "Values" .Values "Chart" .Chart "Release" .Release "component" "udm") | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: oai-udm
  ports:
  - name: sbi
    port: 80
    targetPort: 8080
    protocol: TCP
